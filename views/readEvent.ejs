<!DOCTYPE html>
<html lang="en">
  <%- include("head") %>
  <body>
    <%- include("header") %>

    <!-- wholesermon section -->
    <section class="wholesermon">
      <!-- Content -->
      <div class="wholesermon__content">
        <h2 class="wholesermon__content--title"><%= event.title %></h2>
        <p class="wholesermon__content--para"><%= event.date %></p>
        <p class="wholesermon__content--para"><%= event.description %></p>
      </div>
    </section>

    <!-- Scroll to top button -->
    <div class="scroll"></div>

    <!-- Footer section -->
    <%- include("footer") %>
  </body>
</html>
